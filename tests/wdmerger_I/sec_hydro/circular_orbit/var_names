# Define some useful directories, filenames and variables

merger_makefile:=$(WDMERGER_HOME)/source/GNUmakefile
makefile:=castro_makefile

mass_P:=0.90
mass_S:=0.60

rotational_period:=100.0

num_periods:=50

stop_time:=$$(echo "$(num_periods) * $(rotational_period)" | bc -l)

inputs:=inputs
probin:=probin
